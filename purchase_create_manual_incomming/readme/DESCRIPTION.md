This module allows users to manually create an incoming picking directly from a purchase order, bypassing the standard Odoo procurement flow.
Users can manually generate a receipt (picking) only for purchases that have not been split, ensuring alignment between stock movements and the original procurement logic.
